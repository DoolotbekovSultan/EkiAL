# pages/

## Назначение
Папка `pages` содержит экраны приложения - полноценные страницы фичи, которые представляют собой композицию виджетов и управляют навигацией.

## Используемые библиотеки
- **auto_route** - для type-safe навигации
- **flutter_bloc** - для работы с состоянием

## Структура
**pages/** - 📄 Экраны приложения
- page.dart - 🎯 Базовые страницы и экраны

## Описание файлов

### page.dart
Страница фичи. Содержит:
- Композицию виджетов в экран
- Управление состоянием через BLoC
- Обработку навигационных событий
- Жизненный цикл страницы

**Используемые библиотеки в файле:**
- `auto_route` - @RoutePage, AutoRouter
- `flutter_bloc` - BlocProvider, BlocConsumer, BlocBuilder

## Преимущества

- **Изоляция** - каждая страница независима
- **Навигация** - type-safe переходы между экранами
- **Управление состоянием** - реактивное обновление UI
- **Переиспользуемость** - страницы можно использовать в разных контекстах
- **Тестируемость** - изолированное тестирование экранов

## Пример Page

```dart
@RoutePage()
class Page extends StatelessWidget {
  const Page({super.key});

  @override
  Widget build(BuildContext context) {
    return BlocProvider(
      create: (context) => getIt<Bloc>(),
      child: Scaffold(
        appBar: AppBar(title: const Text('Page')),
        body: BlocConsumer<Bloc, BlocState>(
          listener: (context, state) {
            // Обработка side effects
          },
          builder: (context, state) {
            return state.when(
              loading: () => const CircularProgressIndicator(),
              loaded: (items) => ListView.builder(
                itemCount: items.length,
                itemBuilder: (context, index) => ItemWidget(item: items[index]),
              ),
              error: (message) => ErrorWidget(message: message),
            );
          },
        ),
      ),
    );
  }
}