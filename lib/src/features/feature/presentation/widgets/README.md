# widgets/

## Назначение
Папка `widgets` содержит переиспользуемые UI компоненты фичи - специализированные виджеты, которые инкапсулируют визуальную логику и поведение.

## Используемые библиотеки
- **flutter_hooks** - для упрощения управления состоянием виджетов (опционально)

## Структура
**widgets/** - 🧩 UI компоненты
- widget.dart - 🎯 Базовые виджеты и компоненты

## Описание файлов

### widget.dart
Переиспользуемый UI компонент. Содержит:
- Визуальное представление данных
- Обработку пользовательских жестов
- Анимации и переходы
- Специфичную бизнес-логику отображения

**Используемые библиотеки в файле:**
- `flutter_hooks` - HookWidget, useState, useEffect (если используется)

## Преимущества

- **Переиспользуемость** - компоненты можно использовать в разных страницах
- **Инкапсуляция** - скрытие сложной визуальной логики
- **Тестируемость** - изолированное тестирование компонентов
- **Сопровождаемость** - легкое обновление UI компонентов
- **Консистентность** - единый стиль во всем приложении

## Пример Widget

```dart
class Widget extends StatelessWidget {
  final String title;
  final VoidCallback onPressed;

  const Widget({
    super.key,
    required this.title,
    required this.onPressed,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(16),
      child: ElevatedButton(
        onPressed: onPressed,
        child: Text(title),
      ),
    );
  }
}