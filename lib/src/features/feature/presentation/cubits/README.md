# cubits/

## Назначение
Папка `cubits` содержит простые компоненты управления состоянием с использованием Cubit. Подходит для локального состояния UI без сложной бизнес-логики.

## Используемые библиотеки
- **flutter_bloc** - для реализации Cubit паттерна

## Структура
**cubits/** - ⚡ Простое управление состоянием
- cubit.dart - 🎯 Базовые Cubit компоненты

## Описание файлов

### cubit.dart
Простой компонент управления состоянием. Содержит:
- Локальное состояние UI
- Простые методы изменения состояния
- Обработку пользовательских действий
- Управление формами и валидацией

**Используемые библиотеки в файле:**
- `flutter_bloc` - Cubit, Emitter

## Преимущества

- **Простота** - минимальная boilerplate код
- **Производительность** - легковесные компоненты
- **Локальность** - управление состоянием конкретного виджета
- **Тестируемость** - простые unit тесты
- **Интеграция** - совместимость с BLoC экосистемой

## Пример Cubit

```dart
class Cubit extends Cubit<CubitState> {
  Cubit() : super(const CubitState.initial());

  void updateValue(String value) {
    emit(state.copyWith(value: value));
  }

  void toggleStatus() {
    emit(state.copyWith(isActive: !state.isActive));
  }
}