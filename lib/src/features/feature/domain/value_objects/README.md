# value_objects/

## Назначение
Папка `value_objects` содержит объекты-значения - простые неизменяемые объекты, которые представляют отдельные концепции предметной области с собственной логикой валидации.

## Используемые библиотеки
- **equatable** - для упрощения сравнения объектов (рекомендуется)

## Структура
**value_objects/** - 💎 Объекты-значения
- value_object.dart - 🎯 Базовые объекты-значения

## Описание файлов

### value_object.dart
Базовый объект-значение с валидацией и бизнес-правилами.

**Используемые библиотеки в файле:**
- `equatable` - Equatable для упрощения сравнения объектов

## Преимущества

- **Валидация** - централизованная проверка данных
- **Неизменяемость** - предотвращение случайных изменений
- **Бизнес-правила** - инкапсуляция доменной логики
- **Переиспользуемость** - использование в разных сущностях
- **Type-safe** - строгая типизация значений

## Пример Value Object

```dart
class ValueObject {
  final String value;

  ValueObject(this.value) {
    if (value.isEmpty) {
      throw InvalidValueObjectException();
    }
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ValueObject && value == other.value;

  @override
  int get hashCode => value.hashCode;
}