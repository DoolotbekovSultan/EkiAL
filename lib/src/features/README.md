# features/

## Назначение
Папка `features` содержит функциональные модули приложения, организованные по принципу Feature-First архитектуры. Каждая фича - независимый модуль с собственной бизнес-логикой, данными и интерфейсом.

## Структура
**features/** - 🚀 Функциональные модули
- feature/ - 📦 Конкретная фича
  - data/ - 💾 Слой данных фичи
  - domain/ - 🏛️ Бизнес-логика фичи
  - presentation/ - 🖥️ Пользовательский интерфейс фичи

## Архитектура фичи

Каждая фича следует Clean Architecture и содержит три независимых слоя:

### data/ (Слой данных)
- Реализация работы с данными конкретной фичи
- Источники данных (API, локальная БД)
- Модели данных (DTO)
- Реализации репозиториев

### domain/ (Слой бизнес-логики)  
- Бизнес-сущности и правила конкретной фичи
- Интерфейсы репозиториев
- Сценарии использования (Use Cases)
- Объекты-значения с валидацией

### presentation/ (Слой представления)
- Пользовательский интерфейс конкретной фичи
- Управление состоянием (BLoC/Cubit)
- Страницы и виджеты
- Навигация в рамках фичи

## Преимущества

- **Полная изоляция** - каждая фича содержит все необходимые слои
- **Независимая разработка** - разные команды могут работать над разными фичами
- **Легкое тестирование** - каждый слой фичи тестируется изолированно
- **Простое удаление** - можно удалить фичу без влияния на другие
- **Чистая архитектура** - соблюдение принципов Clean Architecture

## Best Practices

1. **Направление зависимостей** - Presentation → Domain ← Data
2. **Интерфейсы в Domain** - репозитории определяются как интерфейсы
3. **Коммуникация между фичами** - только через интерфейсы
4. **Самодостаточность** - каждая фича содержит все необходимое
5. **Dependency Injection** - для внедрения зависимостей между слоями
6. **Отсутствие прямых зависимостей** - фичи не зависят друг от друга напрямую
7. **Переиспользование через shared/** - общие компоненты выносить в shared слой
8. **Инкапсуляция** - внутренняя реализация фичи скрыта от внешнего мира

## Примеры фич

- **auth/** - аутентификация и авторизация
- **user_profile/** - управление профилем пользователя  
- **products/** - каталог товаров
- **orders/** - управление заказами
- **settings/** - настройки приложения
- **notifications/** - уведомления

## Создание новой фичи

1. Создать папку с именем фичи в `features/`
2. Добавить три стандартных слоя: `data/`, `domain/`, `presentation/`
3. Реализовать слои в правильном порядке: Domain → Data → Presentation
4. Зарегистрировать зависимости в DI
5. Добавить маршруты в навигацию

Каждая фича - это мини-приложение внутри основного приложения, что обеспечивает максимальную гибкость и поддерживаемость кода.