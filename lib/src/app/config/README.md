# config/

## ⚠️ ВАЖНО: МИГРАЦИЯ

Эта папка содержит только файл-экспорт для обратной совместимости.

**Реальная конфигурация находится в:** `core/config/`

## Структура
**config/** - 🔄 Re-export для обратной совместимости
- app_config.dart - Экспортирует конфигурацию из core/config/

## Использование

### ❌ НЕ ИСПОЛЬЗУЙТЕ (устаревший путь):
```dart
import 'package:eki_al/src/app/config/app_config.dart';
```

### ✅ ИСПОЛЬЗУЙТЕ (правильный путь):
```dart
import 'package:eki_al/src/core/config/app_config.dart';
```

## Миграция

Если вы используете старый путь импорта, обновите его на новый:

```dart
// Старый путь (работает, но не рекомендуется)
import 'package:eki_al/src/app/config/app_config.dart';

// Новый путь (рекомендуется)
import 'package:eki_al/src/core/config/app_config.dart';
```

## Причина миграции

Конфигурация была перемещена в `core/config/` для соблюдения архитектурных принципов:
- Core слой не должен зависеть от app слоя
- Конфигурация используется компонентами core (network, di)
- Устранение циклических зависимостей

## Дополнительная информация

Подробная документация по конфигурации находится в:
- [core/config/README.md](../../core/config/README.md)
