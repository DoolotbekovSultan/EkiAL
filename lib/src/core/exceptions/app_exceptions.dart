/// Базовые классы исключений приложения
///
/// ## 🔧 Доступные классы:
/// ### Базовые исключения:
/// - `AppException` - базовый класс всех исключений
/// - `BusinessException` - бизнес-логические ошибки
/// - `ValidationException` - ошибки валидации данных
///
/// ### Специализированные исключения:
/// - `AuthenticationException` - ошибки аутентификации
/// - `AuthorizationException` - ошибки авторизации
/// - `NotFoundException` - ошибки "не найдено"
library;

/// Базовый класс для всех исключений приложения
///
/// Все кастомные исключения должны наследоваться от этого класса.
/// Содержит сообщение для пользователя и стектрейс для отладки.
///
/// Пример использования:
/// ```dart
/// throw AppException('Произошла ошибка');
/// ```
class AppException implements Exception {
  /// Сообщение об ошибке для отображения пользователю
  final String message;

  /// Стек вызовов для отладки (опционально)
  final StackTrace? stackTrace;

  /// Создает исключение с сообщением и стектрейсом
  const AppException(this.message, [this.stackTrace]);

  @override
  String toString() => 'AppException: $message';
}

/// Базовый класс для бизнес-логических исключений
///
/// Используется для ошибок связанных с бизнес-правилами приложения.
/// Например: недостаточно средств, недоступная операция и т.д.
///
/// Пример использования:
/// ```dart
/// throw BusinessException('Недостаточно средств на счете');
/// ```
class BusinessException extends AppException {
  /// Создает бизнес-исключение с сообщением
  const BusinessException(super.message, [super.stackTrace]);

  @override
  String toString() => 'BusinessException: $message';
}

/// Исключение для ошибок валидации данных
///
/// Используется когда введенные пользователем данные не проходят валидацию.
/// Например: неверный формат email, слишком короткий пароль и т.д.
///
/// Пример использования:
/// ```dart
/// throw ValidationException('Email имеет неверный формат');
/// ```
class ValidationException extends AppException {
  /// Создает исключение валидации с сообщением
  const ValidationException(super.message, [super.stackTrace]);

  @override
  String toString() => 'ValidationException: $message';
}

/// Исключение для ошибок аутентификации
///
/// Используется когда пользователь не может быть аутентифицирован.
/// Например: неверный логин/пароль, истекший токен и т.д.
///
/// Пример использования:
/// ```dart
/// throw AuthenticationException('Неверный email или пароль');
/// ```
class AuthenticationException extends AppException {
  /// Создает исключение аутентификации с сообщением
  const AuthenticationException(super.message, [super.stackTrace]);

  @override
  String toString() => 'AuthenticationException: $message';
}

/// Исключение для ошибок авторизации
///
/// Используется когда у пользователя нет прав для выполнения операции.
/// Например: доступ к админским функциям без прав администратора.
///
/// Пример использования:
/// ```dart
/// throw AuthorizationException('Недостаточно прав для выполнения операции');
/// ```
class AuthorizationException extends AppException {
  /// Создает исключение авторизации с сообщением
  const AuthorizationException(super.message, [super.stackTrace]);

  @override
  String toString() => 'AuthorizationException: $message';
}

/// Исключение для случаев когда запрашиваемый ресурс не найден
///
/// Используется когда данные не найдены в базе данных или API.
/// Например: пользователь с указанным ID не существует.
///
/// Пример использования:
/// ```dart
/// throw NotFoundException('Пользователь не найден');
/// ```
class NotFoundException extends AppException {
  /// Создает исключение "не найдено" с сообщением
  const NotFoundException(super.message, [super.stackTrace]);

  @override
  String toString() => 'NotFoundException: $message';
}
