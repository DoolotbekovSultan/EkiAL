# 📋 constants/

## 📖 Описание

Директория `constants` содержит глобальные константы, настройки и перечисления приложения. Все константы централизованы для обеспечения единообразия, легкого обслуживания и избежания "магических чисел" в коде.

## 📁 Структура

```
constants/
├── app_constants.dart      # 🎯 Основные константы приложения
├── api_endpints.dart       # 🌐 API эндпоинты и HTTP константы
├── app_strings.dart        # 📝 Текстовые константы
├── enums.dart              # 🔘 Type-safe перечисления
├── regex_patters.dart      # 🔍 Регулярные выражения
├── storage_keys.dart       # 🔑 Ключи локального хранилища
└── widget_constants.dart   # 🧩 UI константы
```

## 📄 Описание файлов

### 🎯 app_constants.dart

**Назначение:** Основные константы приложения, таймауты, лимиты и базовые настройки.

**Содержит:**
- Название и версия приложения
- Таймауты для API запросов и операций
- Настройки пагинации (размер страницы, начальная страница)
- Лимиты данных (размер файлов, длина строк)
- Настройки безопасности (автовыход, попытки входа)
- Настройки кэширования

**Основные классы:**
- `AppConstants` - абстрактный класс с константами

**Методы и свойства:**
- `appName` - название приложения
- `appVersion` - версия приложения
- `apiTimeout` - таймаут API запросов
- `defaultPageSize` - размер страницы по умолчанию
- `maxImageSizeBytes` - максимальный размер изображения
- `maxLoginAttempts` - максимальное количество попыток входа

**Пример использования:**
```dart
// Использование таймаутов
final timeout = AppConstants.apiTimeout;

// Использование лимитов
if (fileSize > AppConstants.maxImageSizeBytes) {
  throw FileTooLargeException();
}

// Использование настроек пагинации
final pageSize = AppConstants.defaultPageSize;
```

---

### 🌐 api_endpints.dart

**Назначение:** API эндпоинты, HTTP заголовки и статус коды. Базовый URL берется из `AppConfig` через `ApiConstants.baseUrl`.

**Содержит:**
- Относительные пути к API методам
- HTTP заголовки и их значения
- Статус коды HTTP
- Генераторы эндпоинтов для CRUD операций

**Основные классы:**
- `ApiEndpoints` - эндпоинты API
- `ApiHeaders` - HTTP заголовки
- `HttpStatusCodes` - статус коды HTTP

**Методы и свойства:**
- `baseUrl` - базовый URL (прокси к ApiConstants.baseUrl)
- `login`, `register`, `logout` - эндпоинты аутентификации
- `userProfile`, `updateProfile` - эндпоинты пользователя
- `resourceById()`, `resourceList()` - генераторы CRUD эндпоинтов
- `contentType`, `authorization` - HTTP заголовки
- `success`, `badRequest`, `unauthorized` - статус коды

**Пример использования:**
```dart
// Использование эндпоинтов
final loginUrl = ApiEndpoints.login;
final profileUrl = ApiEndpoints.userProfile;

// Генерация CRUD эндпоинтов
final userUrl = ApiEndpoints.resourceById('users', '123');
final productsUrl = ApiEndpoints.resourceList('products');

// Использование заголовков
headers[ApiHeaders.authorization] = 'Bearer $token';
headers[ApiHeaders.contentType] = ApiHeaders.jsonContentType;

// Проверка статус кодов
if (response.statusCode == HttpStatusCodes.success) {
  // обработка успешного ответа
}
```

---

### 📝 app_strings.dart

**Назначение:** Текстовые константы для интернационализации и избежания хардкода строк в коде.

**Содержит:**
- Общие тексты и сообщения
- Тексты кнопок и действий
- Сообщения об ошибках
- Заголовки экранов
- Placeholder тексты

**Основные классы:**
- `AppStrings` - абстрактный класс с текстовыми константами

**Методы и свойства:**
- `appName` - название приложения
- `loading`, `error`, `success` - общие сообщения
- `retry`, `cancel`, `save` - тексты кнопок
- `loginButton`, `registerButton` - тексты кнопок форм
- `errorNetwork`, `errorUnknown` - сообщения об ошибках

**Пример использования:**
```dart
// Использование в UI
Text(AppStrings.appName)
ElevatedButton(
  onPressed: () {},
  child: Text(AppStrings.save),
)

// Использование в сообщениях
showSnackBar(AppStrings.errorNetwork);
```

---

### 🔘 enums.dart

**Назначение:** Type-safe перечисления для статусов, ролей, типов контента и состояний приложения.

**Содержит:**
- Состояния загрузки данных
- Роли пользователей
- Типы контента
- Типы операций загрузки
- Типы уведомлений
- Платформы и ориентации

**Основные перечисления:**
- `LoadState` - состояния загрузки (initial, loading, success, error)
- `UserRole` - роли пользователей (guest, user, admin, moderator)
- `ContentType` - типы контента (text, image, video, audio, document)
- `LoadingType` - типы операций загрузки (initialLoad, refresh, loadMore)
- `NotificationType` - типы уведомлений (info, success, warning, error)
- `AppPlatform` - платформы (android, ios, web)
- `ScreenOrientation` - ориентации экрана (portrait, landscape)

**Пример использования:**
```dart
// Использование состояний
var state = LoadState.loading;
switch (state) {
  case LoadState.initial:
    // начальное состояние
    break;
  case LoadState.loading:
    // загрузка
    break;
  case LoadState.success:
    // успех
    break;
  case LoadState.error:
    // ошибка
    break;
}

// Использование ролей
if (user.role == UserRole.admin) {
  // действия администратора
}

// Использование типов контента
final contentType = ContentType.image;
```

---

### 🔍 regex_patters.dart

**Назначение:** Регулярные выражения для валидации данных.

**Содержит:**
- Паттерны валидации email
- Паттерны валидации телефона
- Паттерны валидации пароля
- Форматы дат
- Числовые форматы

**Пример использования:**
```dart
// Валидация email
final emailRegex = RegExp(RegexPatterns.email);
if (emailRegex.hasMatch(email)) {
  // валидный email
}
```

---

### 🔑 storage_keys.dart

**Назначение:** Ключи для локального хранилища (SharedPreferences, Hive, Isar).

**Содержит:**
- Ключи для пользовательских данных
- Ключи для настроек приложения
- Ключи для кэширования
- Ключи для токенов авторизации

**Пример использования:**
```dart
// Использование ключей
await prefs.setString(StorageKeys.authToken, token);
final token = prefs.getString(StorageKeys.authToken);
```

---

### 🧩 widget_constants.dart

**Назначение:** Константы для UI компонентов.

**Содержит:**
- Размеры элементов
- Отступы и padding
- Радиусы скругления
- Длительности анимаций
- Тени и эффекты

**Пример использования:**
```dart
// Использование в UI
Container(
  padding: EdgeInsets.all(WidgetConstants.defaultPadding),
  borderRadius: BorderRadius.circular(WidgetConstants.borderRadius),
  child: Text('Content'),
)
```

## ✅ Преимущества

- ✅ **Централизованное управление** - все константы в одном месте
- ✅ **Легкое обслуживание** - изменения в одном месте
- ✅ **Type-safe** - перечисления вместо строк
- ✅ **Избежание магических чисел** - все значения именованы
- ✅ **Единообразие** - одинаковые значения используются везде
- ✅ **Документированность** - все константы описаны

## 📚 Best Practices

1. **Не используйте "магические числа"** - выносите все значения в константы
2. **Используйте AppStrings** - вместо хардкода строк в коде
3. **Используйте ApiEndpoints** - для всех URL и эндпоинтов
4. **Используйте перечисления** - вместо строковых констант для статусов
5. **Группируйте константы** - по функциональности в соответствующие файлы
6. **Документируйте константы** - добавляйте комментарии к неочевидным значениям
7. **Используйте константы для UI** - размеры, отступы, радиусы

## 🔗 Связанные модули

- `core/network/api_constants.dart` - сетевые константы (базовый URL)
- `app/config/app_config.dart` - конфигурация приложения (источник baseUrl)
- `core/utils/validators.dart` - валидаторы (используют regex_patters)

## 📝 Примеры использования

```dart
// Комплексный пример использования констант
import 'package:eki_al/src/core/constrants/app_constants.dart';
import 'package:eki_al/src/core/constrants/api_endpints.dart';
import 'package:eki_al/src/core/constrants/app_strings.dart';
import 'package:eki_al/src/core/constrants/enums.dart';

// Использование в сетевом запросе
final dio = Dio(BaseOptions(
  baseUrl: ApiEndpoints.baseUrl,
  connectTimeout: AppConstants.connectTimeout,
  receiveTimeout: AppConstants.receiveTimeout,
));

// Использование в UI
class UserListWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ListView.builder(
      itemCount: items.length,
      padding: EdgeInsets.all(WidgetConstants.defaultPadding),
      itemBuilder: (context, index) {
        return ListTile(
          title: Text(items[index].name),
          subtitle: Text(
            items[index].role == UserRole.admin 
              ? AppStrings.adminRole 
              : AppStrings.userRole,
          ),
        );
      },
    );
  }
}

// Использование в репозитории
class UserRepository {
  Future<List<User>> getUsers({
    int page = AppConstants.initialPage,
    int pageSize = AppConstants.defaultPageSize,
  }) async {
    final url = ApiEndpoints.resourceList('users');
    // запрос с пагинацией
  }
}
```
