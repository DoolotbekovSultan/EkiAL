# constants/

## Назначение
Папка `constants` содержит глобальные константы, настройки и перечисления приложения.

## Структура
**constants/** - 📋 Константы и настройки
- app_constants.dart - 🎯 Основные константы
- api_endpoints.dart - 🌐 API эндпоинты
- enums.dart - 🔘 Перечисления
- app_strings.dart - 📝 Текстовые константы
- regex_patterns.dart - 🔍 Регулярные выражения
- storage_keys.dart - 🔑 Ключи хранилища
- widget_constants.dart - 🧩 UI константы

## Описание файлов

### app_constants.dart
Основные константы приложения.

Содержит:
- название приложения
- версию и build number
- таймауты запросов
- настройки пагинации
- лимиты данных
- базовые настройки приложения

### api_endpoints.dart
Эндпоинты API.

Содержит:
- базовые URL для разных окружений
- пути к API методам
- заголовки запросов
- коды статусов HTTP

### enums.dart
Перечисления приложения.

Содержит:
- статусы операций (loading, success, error)
- типы контента
- роли пользователей
- состояния загрузки
- типы уведомлений

### app_strings.dart
Текстовые константы.

Содержит:
- сообщения об ошибках
- текст кнопок и labels
- заголовки экранов
- placeholder тексты
- системные сообщения

### regex_patterns.dart
Регулярные выражения.

Содержит:
- валидацию email
- валидацию телефона
- валидацию пароля
- форматы дат
- числовые форматы

### storage_keys.dart
Ключи для локального хранилища.

Содержит:
- ключи для SharedPreferences
- ключи для Hive/Isar
- ключи для кэширования

### widget_constants.dart
Константы для UI компонентов.

Содержит:
- размеры элементов
- отступы и padding
- радиусы скругления
- длительности анимаций
- тени и эффекты

## Преимущества

- Централизованное управление константами
- Легкое внесение изменений
- Type-safe перечисления вместо строк
- Избегание "магических чисел" в коде

## Best Practices

1. Не используйте "магические числа" в коде - выносите в константы
2. Для текстов используйте AppStrings вместо хардкода
3. Все URL должны быть в ApiEndpoints
4. Новые перечисления добавляйте в enums.dart
5. Используйте константы для размеров и отступов в UI
6. Регулярные выражения храните в regex_patterns.dart

## Примеры использования

```dart
// Использование констант времени
const timeout = AppConstants.apiTimeout;

// Использование API эндпоинтов
final loginUrl = ApiEndpoints.auth.login;

// Использование перечислений
var userRole = UserRole.admin;

// Использование в сетевых запросах
Dio(BaseOptions(
  connectTimeout: AppConstants.apiTimeout,
  baseUrl: ApiEndpoints.baseUrl,
));

// Использование в UI
Text(
  AppStrings.loginButton,
  style: TextStyle(fontSize: WidgetConstants.buttonFontSize),
);