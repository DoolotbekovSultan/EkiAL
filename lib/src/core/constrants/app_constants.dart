/// 🎯 ОСНОВНЫЕ КОНСТАНТЫ ПРИЛОЖЕНИЯ
///
/// Содержит глобальные настройки, таймауты, лимиты и базовые конфигурации
abstract class AppConstants {
  // ================================
  // 🏷️ ОСНОВНЫЕ НАСТРОЙКИ ПРИЛОЖЕНИЯ
  // ================================

  /// Название приложения
  static const String appName = 'Eki Al';

  /// Версия приложения в формате semver
  static const String appVersion = '1.0.0';

  /// Build number для магазинов приложений
  static const int appBuildNumber = 1;

  // ================================
  // ⏰ ТАЙМАУТЫ И ВРЕМЕННЫЕ НАСТРОЙКИ
  // ================================

  /// Таймаут для API запросов
  static const Duration apiTimeout = Duration(seconds: 30);

  /// Таймаут для установки соединения
  static const Duration connectTimeout = Duration(seconds: 10);

  /// Таймаут для получения ответа
  static const Duration receiveTimeout = Duration(seconds: 30);

  /// Таймаут для debounce операций
  static const Duration debounceTimeout = Duration(milliseconds: 500);

  // ================================
  // 📄 ПАГИНАЦИЯ И ЗАГРУЗКА ДАННЫХ
  // ================================

  /// Количество элементов на странице по умолчанию
  static const int defaultPageSize = 20;

  /// Начальная страница пагинации
  static const int initialPage = 1;

  /// Максимальное количество элементов на странице
  static const int maxPageSize = 100;

  // ================================
  // 📏 ЛИМИТЫ И ОГРАНИЧЕНИЯ ДАННЫХ
  // ================================

  /// Максимальный размер изображения
  static const int maxImageSizeBytes = 5 * 1024 * 1024;

  /// Максимальный размер загружаемого файла
  static const int maxUploadFileSize = 10 * 1024 * 1024;

  /// Максимальная длина строковых полей
  static const int maxStringLength = 255;

  // ================================
  // ⚙️ НАСТРОЙКИ БЕЗОПАСНОСТИ И КЭША
  // ================================

  /// Время автоматического выхода из системы
  static const int autoLogoutMinutes = 30;

  /// Максимальное количество попыток входа
  static const int maxLoginAttempts = 5;

  /// Время жизни кэша в днях
  static const int cacheExpiryDays = 7;

  /// Интервал проверки обновлений
  static const int updateCheckHours = 24;
}
