/// 🔘 ПЕРЕЧИСЛЕНИЯ ПРИЛОЖЕНИЯ
///
/// Содержит type-safe перечисления для статусов, ролей, типов контента и состояний
enum LoadState {
  /// Начальное состояние
  initial,

  /// Загрузка данных
  loading,

  /// Успешное завершение
  success,

  /// Ошибка загрузки
  error,
}

/// Роли пользователей в системе
enum UserRole {
  /// Гостевой доступ
  guest,

  /// Обычный пользователь
  user,

  /// Администратор системы
  admin,

  /// Модератор контента
  moderator,
}

/// Типы контента в приложении
enum ContentType {
  /// Текстовый контент
  text,

  /// Изображения
  image,

  /// Видео контент
  video,

  /// Аудио контент
  audio,

  /// Документы
  document,
}

/// Типы операций загрузки
enum LoadingType {
  /// Первоначальная загрузка
  initialLoad,

  /// Обновление данных
  refresh,

  /// Подгрузка дополнительных данных
  loadMore,
}

/// Типы уведомлений для пользователя
enum NotificationType {
  /// Информационное уведомление
  info,

  /// Успешное выполнение
  success,

  /// Предупреждение
  warning,

  /// Ошибка
  error,
}

/// Поддерживаемые платформы приложения
enum AppPlatform {
  /// Android устройства
  android,

  /// iOS устройства
  ios,

  /// Web версия
  web,
}

/// Ориентация экрана устройства
enum ScreenOrientation {
  /// Портретная ориентация
  portrait,

  /// Альбомная ориентация
  landscape,
}
