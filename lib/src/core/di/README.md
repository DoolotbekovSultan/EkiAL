# di/

## Назначение
Папка `di` содержит конфигурацию Dependency Injection (внедрения зависимостей) для управления зависимостями приложения.

## Используемые библиотеки
- **get_it** - DI контейнер
- **injectable** - кодогенерация для автоматической регистрации зависимостей

## Структура
**di/** - 💉 Dependency Injection
- injector.dart - 🎯 Основной инжектор
- app_module.dart - 📱 Главный модуль
- injector.config.dart - ⚙️ Конфигурация
- feature_modules/ - 🚀 Модули фич

## Описание файлов

### injector.dart
Основной файл инициализации DI контейнера.

Содержит:
- инициализацию GetIt контейнера
- настройку зависимостей
- методы для получения зависимостей

### app_module.dart
Главный модуль зависимостей приложения.

Содержит регистрацию:
- глобальных сервисов
- утилит
- внешних зависимостей
- конфигураций

### injector.config.dart
Автогенерируемый файл конфигурации Injectable.

Содержит сгенерированный код для:
- регистрации зависимостей
- резолвинга зависимостей
- фабричных методов

### feature_modules/
Директория для модулей отдельных фич.

Содержит модули для:
- аутентификации (auth_module.dart)
- пользователей (user_module.dart)
- продуктов (product_module.dart)
- и других фич

## Преимущества

- Централизованное управление зависимостями
- Легкое тестирование с mock зависимостями
- Упрощение рефакторинга
- Автоматическое разрешение зависимостей

## Best Practices

1. Регистрируйте зависимости в соответствующих feature модулях
2. Используйте @singleton для глобальных сервисов
3. Используйте @factory для состояний и BLoC
4. Для асинхронной инициализации используйте @preResolve
5. Разделяйте зависимости по функциональным модулям
6. Избегайте циклических зависимостей

## Примеры использования

```dart
// Регистрация зависимости в модуле
@module
abstract class AuthModule {
  @singleton
  AuthRepository get authRepository => AuthRepositoryImpl();
  
  @factory
  LoginUseCase get loginUseCase => LoginUseCase(getIt());
}

// Получение зависимости в коде
final authRepository = getIt<AuthRepository>();
final loginUseCase = getIt<LoginUseCase>();

// Использование в BLoC
class AuthBloc extends Bloc<AuthEvent, AuthState> {
  final LoginUseCase loginUseCase;

  AuthBloc(this.loginUseCase) : super(AuthInitial());
}

// Типы регистрации зависимостей
// Один экземпляр на все приложение
@singleton
Dio get dio => Dio();

// Ленивая инициализация при первом использовании
@lazySingleton
DatabaseService get databaseService => DatabaseService();

// Новый экземпляр при каждом запросе
@factory
AuthBloc get authBloc => AuthBloc(getIt());

// Асинхронная инициализация перед использованием
@preResolve
Future<SharedPreferences> get prefs => SharedPreferences.getInstance();

// Процесс настройки
// 1. Создать модули с зависимостями
// 2. Пометить классы аннотациями @injectable, @singleton и т.д.
// 3. Запустить генерацию кода:
flutter pub run build_runner build

// 4. Инициализировать DI в main.dart:
void main() async {
  WidgetsFlutterBinding.ensureInitialized();
  await configureDependencies();
  runApp(MyApp());
}