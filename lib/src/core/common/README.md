# common/

## Назначение
Папка `common` содержит базовые классы, абстракции и общие утилиты, которые используются во всем приложении.

## Используемые библиотеки
- **flutter_bloc** - базовые классы для BLoC
- **freezed** - модели данных и состояния
- **equatable** - сравнение объектов
- **dartz** - функциональные типы (Either)

## Структура
**common/** - 🔧 Базовые классы
- base_repository.dart - 📚 Базовый репозиторий
- base_usecase.dart - 🎯 Базовый UseCase  
- base_bloc.dart - 🎛️ Базовый BLoC
- base_model.dart - 📊 Базовая модель
- base_state.dart - 📈 Базовое состояние
- base_event.dart - ⚡ Базовое событие
- base_response.dart - 📨 Базовый ответ

## Описание файлов

### base_repository.dart
Базовый класс для всех репозиториев приложения.

**Использует:** `dartz` (Either), `exceptions/failure.dart`

Содержит общие методы для:
- CRUD операций с Either результатом
- обработки ошибок и исключений  
- кэширования данных
- валидации идентификаторов

### base_usecase.dart
Базовый класс для сценариев использования (Use Cases).

**Использует:** `dartz` (Either), `exceptions/failure.dart`

Содержит:
- шаблон для UseCase с Either результатом
- обработку исключений
- валидацию входных параметров
- стандартную структуру выполнения

### base_bloc.dart
Базовый класс для BLoC компонентов.

**Использует:** `flutter_bloc`

Содержит общую логику для:
- обработки событий
- управления подписками  
- логирования изменений состояния
- обработки ошибок

### base_model.dart
Базовый класс для моделей данных.

**Использует:** `freezed`, `equatable`, `json_annotation`

Содержит:
- базовые методы сериализации/десериализации
- методы сравнения (==, hashCode)
- валидацию данных
- преобразование в/из JSON

### base_state.dart
Базовый класс для состояний BLoC.

**Использует:** `freezed`, `equatable`

Содержит общие свойства:
- флаги загрузки
- обработка ошибок
- общие методы для работы с состоянием

### base_event.dart
Базовый класс для событий BLoC.

**Использует:** `freezed`, `equatable`

Содержит общую структуру для всех событий приложения.

### base_response.dart
Базовый класс для ответов API.

**Использует:** `freezed`, `json_annotation`

Содержит общую структуру:
- статус ответа
- данные
- сообщения об ошибках
- мета-информация

## Преимущества

- Единообразие - все компоненты следуют единому шаблону
- Сокращение дублирования кода
- Упрощение тестирования
- Легкая поддерживаемость

## Best Practices

1. Все репозитории должны наследоваться от BaseRepository
2. Все Use Cases должны наследоваться от BaseUseCase  
3. Для сложных BLoC используйте BaseBloc как основу
4. Новые базовые классы добавляйте только при явной необходимости

## Примеры использования

```dart
// Наследование от базового репозитория
class UserRepository extends BaseRepository<UserEntity> {
  @override
  Future<Either<Failure, UserEntity>> getById(String id) async {
    // специфичная реализация для пользователей
  }
}

// Использование базового UseCase
class GetUserUseCase extends BaseUseCase<String, UserEntity> {
  @override
  Future<Either<Failure, UserEntity>> execute(String userId) async {
    // логика получения пользователя
  }
}