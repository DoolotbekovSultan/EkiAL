# 🌐 network/

## 📖 Описание

Директория `network` содержит конфигурацию сетевого слоя приложения, включая HTTP клиент, интерцепторы, утилиты для работы с API и модели ответов. Все сетевые операции централизованы для обеспечения единообразия, обработки ошибок и легкого тестирования.

## 📁 Структура

```
network/
├── api_constants.dart           # 📋 Константы сетевого слоя
├── dio_client.dart              # 🎯 Основной HTTP клиент
├── network_info.dart            # 📡 Информация о сети
├── interceptors/                # 🔄 Обработчики запросов
│   ├── auth_interceptor.dart    # 🔐 Авторизация
│   ├── logging_interceptor.dart # 📝 Логирование
│   ├── retry_interceptor.dart   # 🔁 Повтор запросов
│   └── error_interceptor.dart   # ⚠️ Обработка ошибок
└── response_models/             # 📊 Модели ответов
    ├── base_response.dart       # 📦 Базовая модель
    └── paginated_response.dart  # 📄 Пагинированный ответ
```

## 📄 Описание файлов

### 📋 api_constants.dart

**Назначение:** Константы для сетевых запросов, динамически получаемые из `AppConfig`.

**Содержит:**
- Базовый URL (из AppConfig)
- Таймауты соединения
- Заголовки по умолчанию
- Ключи аутентификации
- Статус коды HTTP
- Настройки повтора запросов
- Пути API
- Флаги логирования

**Основные классы:**
- `ApiConstants` - класс с константами

**Методы и свойства:**
- `baseUrl` - базовый URL (из AppConfig)
- `connectTimeout`, `receiveTimeout`, `sendTimeout` - таймауты
- `defaultHeaders`, `multipartHeaders` - заголовки
- `authHeaderKey`, `bearerPrefix` - аутентификация
- `successStatusCodes`, `authErrorStatusCodes` - статус коды
- `maxRetryAttempts`, `retryDelay` - настройки повтора
- `enableRequestLogging`, `enableResponseLogging` - логирование

**Пример использования:**
```dart
final dio = Dio(BaseOptions(
  baseUrl: ApiConstants.baseUrl,
  connectTimeout: ApiConstants.connectTimeout,
  headers: ApiConstants.defaultHeaders,
));

if (ApiConstants.successStatusCodes.contains(statusCode)) {
  // обработка успешного ответа
}
```

---

### 🎯 dio_client.dart

**Назначение:** Основной HTTP клиент на основе Dio с предустановленной конфигурацией.

**Содержит:**
- Базовую конфигурацию Dio
- Настройку таймаутов из ApiConstants
- Добавление интерцепторов
- Методы для HTTP запросов (GET, POST, PUT, DELETE, PATCH)
- Методы для загрузки файлов
- Утилиты для работы с URL

**Основные классы:**
- `DioClient` - основной HTTP клиент

**Методы:**
- `get<T>()` - GET запрос
- `post<T>()` - POST запрос
- `put<T>()` - PUT запрос
- `delete<T>()` - DELETE запрос
- `patch<T>()` - PATCH запрос
- `download()` - загрузка файла
- `postFormData<T>()` - отправка FormData
- `setBaseUrl()` - изменение базового URL
- `setAuthToken()` - установка токена авторизации
- `clearAuthToken()` - очистка токена

**Пример использования:**
```dart
final dioClient = DioClient();

// GET запрос
final response = await dioClient.get<User>('/users/1');

// POST запрос
final created = await dioClient.post<User>(
  '/users',
  data: userData,
);

// Установка токена
dioClient.setAuthToken(token);

// Загрузка файла
await dioClient.download(
  '/files/document.pdf',
  '/local/path/document.pdf',
);
```

---

### 📡 network_info.dart

**Назначение:** Утилита для проверки сетевого подключения и мониторинга состояния сети.

**Содержит:**
- Проверку доступности интернета
- Мониторинг изменения состояния сети
- Уведомления о потере соединения
- Определение типа подключения (WiFi, Mobile, None)

**Пример использования:**
```dart
final networkInfo = NetworkInfo();

// Проверка подключения
final hasConnection = await networkInfo.isConnected;

// Подписка на изменения
networkInfo.onStatusChange.listen((status) {
  if (status == ConnectivityResult.none) {
    // нет подключения
  }
});
```

---

### 🔄 interceptors/

Директория с интерцепторами для обработки запросов и ответов.

#### 🔐 auth_interceptor.dart

**Назначение:** Интерцептор для автоматической авторизации запросов.

**Содержит:**
- Добавление токена авторизации в заголовки
- Обновление access token при истечении
- Обработку 401 ошибок (unauthorized)
- Логику refresh token

**Пример использования:**
```dart
dio.interceptors.add(AuthInterceptor(
  tokenProvider: () => getToken(),
  onTokenExpired: () => refreshToken(),
));
```

---

#### 📝 logging_interceptor.dart

**Назначение:** Интерцептор для логирования сетевых операций.

**Содержит:**
- Логирование всех исходящих запросов
- Логирование входящих ответов
- Форматирование логов для удобства чтения
- Фильтрацию конфиденциальных данных
- Измерение времени выполнения запросов

**Особенности:**
- В production логирует только ошибки
- В development логирует все операции
- Автоматически фильтрует пароли и токены

**Пример использования:**
```dart
dio.interceptors.add(LoggingInterceptor());
```

---

#### 🔁 retry_interceptor.dart

**Назначение:** Интерцептор для автоматического повтора неудачных запросов.

**Содержит:**
- Логику повторения при сетевых ошибках
- Настройки количества попыток (из ApiConstants)
- Задержки между попытками
- Экспоненциальный backoff
- Исключения для определенных типов ошибок

**Пример использования:**
```dart
dio.interceptors.add(RetryInterceptor(
  maxAttempts: ApiConstants.maxRetryAttempts,
  retryDelay: ApiConstants.retryDelay,
));
```

---

#### ⚠️ error_interceptor.dart

**Назначение:** Интерцептор для обработки и преобразования ошибок.

**Содержит:**
- Преобразование DioError в кастомные исключения
- Обработку сетевых ошибок
- Форматирование сообщений об ошибках
- Классификацию ошибок по типам

**Пример использования:**
```dart
dio.interceptors.add(ErrorInterceptor());
```

---

### 📊 response_models/

Модели для стандартизированных ответов API.

#### 📦 base_response.dart

**Назначение:** Базовая модель ответа API.

**Содержит:**
- Статус ответа (success, error)
- Данные ответа
- Сообщения об ошибках
- Мета-информацию (timestamp, version)

**Пример использования:**
```dart
final response = BaseResponse<User>.fromJson(json);
if (response.success) {
  final user = response.data;
} else {
  final error = response.error;
}
```

---

#### 📄 paginated_response.dart

**Назначение:** Модель для пагинированных ответов.

**Содержит:**
- Список элементов
- Информацию о пагинации (page, pageSize, total)
- Методы для работы со страницами
- Проверку наличия следующей/предыдущей страницы

**Пример использования:**
```dart
final response = PaginatedResponse<User>.fromJson(json);
final users = response.items;
final hasNextPage = response.hasNextPage;
final nextPage = response.nextPage;
```

---

## ✅ Преимущества

- ✅ **Централизованная конфигурация** - все сетевые настройки в одном месте
- ✅ **Единообразная обработка ошибок** - стандартизированные исключения
- ✅ **Легкое тестирование** - возможность подмены mock клиентами
- ✅ **Гибкая настройка** - модульная система интерцепторов
- ✅ **Автоматическое логирование** - все запросы логируются в dev режиме
- ✅ **Автоматическая авторизация** - токены добавляются автоматически
- ✅ **Повтор запросов** - автоматический retry при сетевых ошибках

## 📚 Best Practices

1. **Используйте интерцепторы** для cross-cutting concerns (auth, logging, retry)
2. **Настройте правильные таймауты** для разных типов запросов
3. **Обрабатывайте ошибки** на уровне сетевого слоя
4. **Используйте retry логику** для ненадежных сетей
5. **Логируйте запросы** только в dev режиме
6. **Проверяйте наличие интернета** перед запросами
7. **Используйте стандартизированные модели** ответов
8. **Настройте кэширование** для часто запрашиваемых данных
9. **Фильтруйте конфиденциальные данные** в логах
10. **Используйте ApiConstants** для всех констант

## 🔗 Связанные модули

- `app/config/app_config.dart` - источник базового URL и настроек
- `core/constrants/api_endpints.dart` - относительные пути API
- `core/exceptions/` - кастомные исключения для сетевых ошибок
- `core/di/` - регистрация DioClient в DI контейнере

## 📝 Примеры использования

```dart
// Создание клиента
final dioClient = DioClient();

// Выполнение GET запроса
final response = await dioClient.get<User>(
  ApiEndpoints.userProfile,
);

// Выполнение POST запроса с данными
final created = await dioClient.post<User>(
  ApiEndpoints.resourceCreate('users'),
  data: {
    'name': 'John Doe',
    'email': 'john@example.com',
  },
);

// Загрузка файла
await dioClient.download(
  '/files/document.pdf',
  '/local/path/document.pdf',
  onReceiveProgress: (received, total) {
    final progress = received / total;
    print('Progress: ${(progress * 100).toStringAsFixed(0)}%');
  },
);

// Проверка сетевого подключения
final networkInfo = NetworkInfo();
if (!await networkInfo.isConnected) {
  throw NoInternetException();
}

// Обработка ошибок
try {
  final user = await dioClient.get<User>('/users/1');
} on SocketException catch (_) {
  // Нет интернета
  showError(AppStrings.errorNetwork);
} on HttpException catch (e) {
  // Ошибка сервера
  showError(e.message);
} on DioException catch (e) {
  // Ошибка Dio
  if (e.response?.statusCode == HttpStatusCodes.unauthorized) {
    // Неавторизован - обновить токен
    await refreshToken();
  }
}
```

## 🛠️ Используемые библиотеки

- **dio** - HTTP клиент с интерцепторами
- **connectivity_plus** - проверка сетевого подключения
- **retrofit** (опционально) - генерация API клиентов
