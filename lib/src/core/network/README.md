# network/

## Назначение
Папка `network` содержит конфигурацию сетевого слоя приложения, включая HTTP клиент, интерцепторы и утилиты для работы с API.

## Используемые библиотеки
- **dio** - HTTP клиент с интерцепторами
- **retrofit** - генерация API клиентов
- **connectivity_plus** - проверка сетевого подключения

## Структура
**network/** - 🌐 Сетевая конфигурация
- dio_client.dart - 🎯 Основной HTTP клиент
- api_constants.dart - 📋 Константы API
- interceptors/ - 🔄 Обработчики запросов
- network_info.dart - 📡 Информация о сети
- response_models/ - 📊 Модели ответов

## Описание файлов

### dio_client.dart
Основной HTTP клиент на основе Dio.

Содержит:
- базовую конфигурацию Dio
- настройку таймаутов
- добавление интерцепторов
- методы для HTTP запросов

### api_constants.dart
Константы для сетевых запросов.

Содержит:
- базовые URL для разных окружений
- заголовки по умолчанию
- коды статусов HTTP
- таймауты соединения

### interceptors/
Директория с интерцепторами для обработки запросов и ответов.

#### auth_interceptor.dart
Интерцептор для авторизации.

Содержит:
- добавление токена авторизации
- обновление access token
- обработку 401 ошибок

#### logging_interceptor.dart
Интерцептор для логирования.

Содержит:
- логирование запросов и ответов
- форматирование логов
- фильтрацию конфиденциальных данных

#### retry_interceptor.dart
Интерцептор для повторения запросов.

Содержит:
- логику повторения при неудачных запросах
- настройки количества попыток
- задержки между попытками

#### error_interceptor.dart
Интерцептор для обработки ошибок.

Содержит:
- преобразование DioError в кастомные исключения
- обработку сетевых ошибок
- форматирование сообщений об ошибках

### network_info.dart
Утилита для проверки сетевого подключения.

Содержит:
- проверку доступности интернета
- мониторинг изменения состояния сети
- уведомления о потере соединения

### response_models/
Модели для стандартизированных ответов API.

#### base_response.dart
Базовая модель ответа API.

Содержит:
- статус ответа
- данные
- сообщения об ошибках
- мета-информацию

#### paginated_response.dart
Модель для пагинированных ответов.

Содержит:
- список элементов
- информацию о пагинации
- методы для работы со страницами

## Преимущества

- Централизованная конфигурация сетевого слоя
- Единообразная обработка ошибок
- Легкое тестирование с mock клиентами
- Гибкая настройка через интерцепторы
- Автоматическое логирование запросов

## Best Practices

1. Используйте интерцепторы для cross-cutting concerns
2. Настройте правильные таймауты для разных типов запросов
3. Обрабатывайте ошибки на уровне сетевого слоя
4. Используйте retry логику для ненадежных сетей
5. Логируйте запросы в dev режиме
6. Проверяйте наличие интернета перед запросами
7. Используйте стандартизированные модели ответов
8. Настройте кэширование для часто запрашиваемых данных

## Примеры использования

```dart
// Создание клиента
final dioClient = DioClient();

// Выполнение запроса
final response = await dioClient.get<User>('/users/1');

// Использование с Retrofit
@RestApi()
abstract class UserApiService {
  factory UserApiService(Dio dio) = _UserApiService;
  
  @GET('/users/{id}')
  Future<UserResponse> getUser(@Path('id') String id);
}

// Проверка сетевого подключения
final hasConnection = await networkInfo.isConnected;

// Обработка ошибок
try {
  final user = await userRepository.getUser('1');
} on SocketException catch (_) {
  // Нет интернета
} on HttpException catch (e) {
  // Ошибка сервера
}

// Конфигурация Dio
BaseOptions(
  baseUrl: ApiConstants.baseUrl,
  connectTimeout: const Duration(seconds: 30),
  receiveTimeout: const Duration(seconds: 30),
  sendTimeout: const Duration(seconds: 30),
  headers: {
    'Content-Type': 'application/json',
    'Accept': 'application/json',
  },
)