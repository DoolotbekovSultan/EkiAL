# 🎯 core/

## 📖 Описание

Директория `core` содержит фундаментальные компоненты приложения, используемые во всем проекте. Это базовый слой, который обеспечивает единообразие, переиспользуемость и централизованное управление конфигурациями.

## 📁 Структура

```
core/
├── common/          # 🔧 Базовые классы и общие утилиты
├── constrants/      # 📋 Глобальные константы и настройки
├── di/              # 💉 Dependency Injection
├── exceptions/      # ⚠️ Обработка ошибок и исключения
├── extensions/      # 🔌 Расширения для Dart классов
├── monitoring/      # 📊 Мониторинг и аналитика
├── network/         # 🌐 Сетевая конфигурация и HTTP клиент
├── theme/           # 🎨 Тема приложения и стили
└── utils/           # 🛠️ Вспомогательные функции и утилиты
```

## 📄 Описание директорий

### 🔧 common/

**Назначение:** Базовые классы и общие утилиты для всего приложения.

**Содержит:**
- Базовые классы репозиториев
- Базовые классы use cases
- Базовые классы BLoC/Cubit
- Базовые классы моделей
- Общие утилиты и хелперы

**Основные компоненты:**
- `BaseRepository` - базовый класс для репозиториев
- `BaseUseCase` - базовый класс для use cases
- `BaseBloc` - базовый класс для BLoC
- `BaseModel` - базовая модель данных

**Пример использования:**
```dart
class UserRepository extends BaseRepository<UserEntity> {
  @override
  Future<Either<Failure, UserEntity>> getById(String id) async {
    // реализация
  }
}
```

**Подробнее:** [common/README.md](common/README.md)

---

### 📋 constrants/

**Назначение:** Глобальные константы, настройки и перечисления приложения.

**Содержит:**
- Основные константы приложения (app_constants.dart)
- API эндпоинты и HTTP константы (api_endpints.dart)
- Текстовые константы (app_strings.dart)
- Type-safe перечисления (enums.dart)
- Регулярные выражения (regex_patters.dart)
- Ключи хранилища (storage_keys.dart)
- UI константы (widget_constants.dart)

**Основные компоненты:**
- `AppConstants` - основные константы
- `ApiEndpoints` - API эндпоинты
- `AppStrings` - текстовые константы
- `LoadState`, `UserRole` - перечисления

**Пример использования:**
```dart
final timeout = AppConstants.apiTimeout;
final loginUrl = ApiEndpoints.login;
Text(AppStrings.appName);
```

**Подробнее:** [constrants/README.md](constrants/README.md)

---

### 💉 di/

**Назначение:** Dependency Injection (внедрение зависимостей) для управления зависимостями.

**Содержит:**
- Конфигурацию DI контейнера (injector.dart)
- Главный модуль зависимостей (app_module.dart)
- Автогенерируемую конфигурацию (injector.config.dart)
- Модули отдельных фич (feature_modules/)

**Основные компоненты:**
- `getIt` - глобальный экземпляр GetIt
- `configureDependencies()` - инициализация DI
- `AppModule` - главный модуль зависимостей
- `DI` - упрощенный интерфейс для работы с DI

**Пример использования:**
```dart
await configureDependencies(environment: Environment.dev);
final dio = getIt<Dio>();
final repository = DI.get<UserRepository>();
```

**Подробнее:** [di/README.md](di/README.md)

---

### ⚠️ exceptions/

**Назначение:** Обработка ошибок и кастомные исключения приложения.

**Содержит:**
- Кастомные исключения приложения
- Классы Failure для обработки ошибок
- Обработчики исключений
- Сетевые исключения
- Валидационные исключения

**Основные компоненты:**
- `AppException` - базовое исключение
- `NetworkException` - сетевые ошибки
- `ValidationException` - ошибки валидации
- `Failure` - класс для обработки ошибок

**Пример использования:**
```dart
try {
  final user = await userRepository.getUser('1');
} on NetworkException catch (e) {
  // обработка сетевой ошибки
} on ValidationException catch (e) {
  // обработка ошибки валидации
}
```

**Подробнее:** [exceptions/README.md](exceptions/README.md)

---

### 🔌 extensions/

**Назначение:** Расширения для стандартных Dart классов.

**Содержит:**
- Расширения для String
- Расширения для DateTime
- Расширения для List
- Расширения для BuildContext
- Расширения для num/int/double

**Основные компоненты:**
- `StringExtensions` - расширения для строк
- `DateTimeExtensions` - расширения для дат
- `ListExtensions` - расширения для списков
- `BuildContextExtensions` - расширения для контекста

**Пример использования:**
```dart
if (email.isValidEmail) {
  // валидный email
}
final formattedDate = DateTime.now().toFormattedString();
final isEmpty = list.isNullOrEmpty;
```

**Подробнее:** [extensions/README.md](extensions/README.md)

---

### 📊 monitoring/

**Назначение:** Мониторинг, аналитика и трекинг событий приложения.

**Содержит:**
- Сервис аналитики (analytics_service.dart)
- Провайдеры аналитики (Firebase, Sentry, Console)
- Трекинг событий и ошибок
- Мониторинг производительности

**Основные компоненты:**
- `AnalyticsService` - абстрактный сервис аналитики
- `UniversalAnalyticsService` - универсальная реализация
- `AnalyticsProvider` - интерфейс провайдеров
- `ConsoleAnalyticsProvider` - консольный провайдер
- `MockAnalyticsProvider` - mock провайдер для тестов

**Пример использования:**
```dart
final analytics = UniversalAnalyticsService(
  providers: [ConsoleAnalyticsProvider()],
);
await analytics.initialize();
analytics.trackEvent('button_click', {'button_id': 'submit'});
```

**Подробнее:** [monitoring/README.md](monitoring/README.md)

---

### 🌐 network/

**Назначение:** Сетевая конфигурация и HTTP клиент для работы с API.

**Содержит:**
- Константы сетевого слоя (api_constants.dart)
- Основной HTTP клиент (dio_client.dart)
- Интерцепторы запросов (interceptors/)
- Модели ответов (response_models/)
- Утилиты для работы с сетью

**Основные компоненты:**
- `ApiConstants` - константы сетевого слоя
- `DioClient` - основной HTTP клиент
- `AuthInterceptor` - интерцептор авторизации
- `LoggingInterceptor` - интерцептор логирования
- `RetryInterceptor` - интерцептор повтора запросов

**Пример использования:**
```dart
final dioClient = DioClient();
final response = await dioClient.get<User>('/users/1');
```

**Подробнее:** [network/README.md](network/README.md)

---

### 🎨 theme/

**Назначение:** Тема приложения, стили и визуальные константы.

**Содержит:**
- Цветовую палитру
- Текстовые стили
- Конфигурации тем (светлая/темная)
- Контроллер темы
- UI константы

**Основные компоненты:**
- `AppColors` - цветовая палитра
- `AppTextStyles` - текстовые стили
- `AppTheme` - конфигурация темы
- `ThemeController` - контроллер темы

**Пример использования:**
```dart
Container(
  color: AppColors.primary,
  child: Text(
    'Hello',
    style: AppTextStyles.headlineLarge,
  ),
)
```

**Подробнее:** [theme/README.md](theme/README.md)

---

### 🛠️ utils/

**Назначение:** Вспомогательные функции и утилиты для работы с данными.

**Содержит:**
- Валидаторы данных (validators.dart)
- Форматтеры (formatters.dart)
- Утилиты для дат (date_utils.dart)
- Утилиты для устройств (device_utils.dart)
- Утилиты для денег (money_utils.dart)
- Утилиты для изображений (image_utils.dart)
- Debouncer (debouncer.dart)
- Утилиты логирования (log_utils.dart)

**Основные компоненты:**
- `Validators` - валидаторы данных
- `DateUtils` - утилиты для дат
- `MoneyUtils` - утилиты для денег
- `Debouncer` - debounce для операций
- `Log` - утилита логирования

**Пример использования:**
```dart
final isValidEmail = Validators.isEmail('test@example.com');
final formattedDate = DateUtils.formatDateTime(DateTime.now());
final formattedPrice = MoneyUtils.formatPrice(1500.50);
final debouncer = Debouncer(milliseconds: 500);
```

**Подробнее:** [utils/README.md](utils/README.md)

---

## ✅ Преимущества

- ✅ **Единообразие** - единый стиль кодовой базы
- ✅ **Переиспользуемость** - компоненты используются во всем проекте
- ✅ **Централизованное управление** - конфигурации в одном месте
- ✅ **Упрощение тестирования** - легко подменять mock объектами
- ✅ **Легкая поддерживаемость** - четкая структура и документация
- ✅ **Независимость** - Core Layer не зависит от других слоев
- ✅ **Type-safe** - использование перечислений и типов

## 📚 Best Practices

1. **Используйте базовые классы** из common/ для единообразия
2. **Все константы выносите** в constrants/
3. **Новые зависимости регистрируйте** в di/
4. **Кастомные исключения создавайте** в exceptions/
5. **Часто используемые операции выносите** в extensions/ и utils/
6. **Следите за независимостью** Core Layer от других слоев
7. **Обеспечьте покрытие тестами** ключевых компонентов Core
8. **Используйте документацию** - все модули должны быть задокументированы
9. **Следуйте единому стилю** комментирования и документации
10. **Не дублируйте код** - используйте существующие утилиты

## 🔗 Связанные модули

- `app/` - использует Core Layer для конфигурации и утилит
- `features/` - использует Core Layer для базовых классов и утилит
- `shared/` - использует Core Layer для общих компонентов

## 📝 Примеры использования

### Использование базовых классов

```dart
// Репозиторий
class UserRepository extends BaseRepository<UserEntity> {
  @override
  Future<Either<Failure, UserEntity>> getById(String id) async {
    try {
      final user = await dataSource.getUser(id);
      return Right(user);
    } catch (e) {
      return Left(ServerFailure(e.toString()));
    }
  }
}

// Use Case
class GetUserUseCase extends BaseUseCase<String, UserEntity> {
  final UserRepository repository;
  
  GetUserUseCase(this.repository);
  
  @override
  Future<Either<Failure, UserEntity>> call(String params) {
    return repository.getById(params);
  }
}
```

### Использование констант

```dart
// Константы времени
const timeout = AppConstants.apiTimeout;

// API эндпоинты
final loginUrl = ApiEndpoints.login;

// Текстовые константы
Text(AppStrings.appName);

// Перечисления
var state = LoadState.loading;
if (user.role == UserRole.admin) {
  // действия администратора
}
```

### Использование DI

```dart
// Инициализация
await configureDependencies(environment: Environment.dev);

// Получение зависимостей
final authRepository = getIt<AuthRepository>();
final loginUseCase = DI.get<LoginUseCase>();
```

### Использование расширений

```dart
// Валидация email
if (email.isValidEmail) {
  // валидный email
}

// Форматирование даты
final formattedDate = DateTime.now().toFormattedString();

// Проверка списка
if (list.isNullOrEmpty) {
  // список пуст
}
```

### Обработка ошибок

```dart
try {
  final user = await userRepository.getUser('1');
} on NetworkException catch (e) {
  // обработка сетевой ошибки
  showError(AppStrings.errorNetwork);
} on ValidationException catch (e) {
  // обработка ошибки валидации
  showError(e.message);
}
```

### Использование темы

```dart
Container(
  color: AppColors.primary,
  child: Text(
    'Hello',
    style: AppTextStyles.headlineLarge,
  ),
)
```

### Использование утилит

```dart
// Валидация
final isValidPhone = Validators.isPhone('+79991234567');

// Форматирование денег
final formattedPrice = MoneyUtils.formatPrice(1500.50);

// Debounce
final debouncer = Debouncer(milliseconds: 500);
debouncer.run(() {
  // выполнение операции
});
```

## 📚 Дополнительная документация

Для получения детальной информации о каждом модуле Core Layer, обратитесь к соответствующим README файлам:

- [common/](common/README.md) - Базовые классы и утилиты
- [constrants/](constrants/README.md) - Глобальные константы и настройки
- [di/](di/README.md) - Dependency Injection
- [exceptions/](exceptions/README.md) - Обработка ошибок и исключения
- [extensions/](extensions/README.md) - Расширения для Dart классов
- [monitoring/](monitoring/README.md) - Мониторинг и аналитика
- [network/](network/README.md) - Сетевая конфигурация и HTTP клиент
- [theme/](theme/README.md) - Тема приложения и стили
- [utils/](utils/README.md) - Вспомогательные функции и утилиты
