// ================================
// 📏 GAP - СИСТЕМА ДИЗАЙН-ТОКЕНОВ ДЛЯ ОТСТУПОВ
// ================================

import 'package:flutter/material.dart';

/// 🎯 СИСТЕМА ДИЗАЙН-ТОКЕНОВ ДЛЯ ЕДИНООБРАЗНЫХ ОТСТУПОВ
///
/// Реализует 8px grid систему для визуальной согласованности в приложении.
///
/// ## 🎯 ЛУЧШИЕ ПРАКТИКИ:
/// - Используйте токены отступов вместо магических чисел
/// - Сохраняйте вертикальный ритм с помощью consistent spacing
/// - Используйте Gap виджеты вместо SizedBox для семантических отступов
/// - Следуйте базовой единице 8px для всех измерений
///
/// ## 📐 ШКАЛА ОТСТУПОВ (база 8px):
/// - xs: 2px  (0.25 × 8px) - минимальный отступ
/// - sm: 4px  (0.5 × 8px)  - плотное расстояние
/// - md: 8px  (1 × 8px)    - базовая единица
/// - lg: 12px (1.5 × 8px)  - комфортное расстояние
/// - xl: 16px (2 × 8px)    - отступ между секциями
/// - xxl: 24px (3 × 8px)   - большое разделение
class Gap {
  Gap._();

  // ================================
  // 📊 ДИЗАЙН-ТОКЕНЫ ОТСТУПОВ - ДИЗАЙН-СИСТЕМА
  // ================================

  /// Нулевой отступ - для edge cases и сброса
  static const double zero = 0;

  /// Очень маленький отступ (2px) - минимальное разделение между тесно связанными элементами
  static const double xs = 2;

  /// Маленький отступ (4px) - плотная группировка связанных элементов
  static const double sm = 4;

  /// Средний отступ (8px) - базовая единица, стандартное расстояние между связанными элементами
  static const double md = 8;

  /// Большой отступ (12px) - комфортное разделение между группами контента
  static const double lg = 12;

  /// Очень большой отступ (16px) - разделение секций, между основными блоками контента
  static const double xl = 16;

  /// 2x большой отступ (24px) - значительное разделение между различными секциями
  static const double xxl = 24;

  /// 3x большой отступ (32px) - максимальное разделение для визуальной иерархии
  static const double xxxl = 32;

  /// 4x большой отступ (48px) - экстремальное разделение для hero секций
  static const double xxxxl = 48;

  // ================================
  // 🎯 ВЕРТИКАЛЬНЫЕ ОТСТУПЫ - ВИДЖЕТЫ
  // ================================

  /// Вертикальный очень маленький отступ (2px) - минимальное вертикальное разделение
  static const SizedBox vxs = SizedBox(height: xs);

  /// Вертикальный маленький отступ (4px) - плотное вертикальное расстояние
  static const SizedBox vsm = SizedBox(height: sm);

  /// Вертикальный средний отступ (8px) - стандартное вертикальное расстояние
  static const SizedBox vmd = SizedBox(height: md);

  /// Вертикальный большой отступ (12px) - комфортное вертикальное разделение
  static const SizedBox vlg = SizedBox(height: lg);

  /// Вертикальный очень большой отступ (16px) - разделение вертикальных секций
  static const SizedBox vxl = SizedBox(height: xl);

  /// Вертикальный 2x большой отступ (24px) - значительное вертикальное разделение
  static const SizedBox vxxl = SizedBox(height: xxl);

  /// Вертикальный 3x большой отступ (32px) - максимальное вертикальное разделение
  static const SizedBox vxxxl = SizedBox(height: xxxl);

  /// Вертикальный 4x большой отступ (48px) - экстремальное вертикальное разделение
  static const SizedBox vxxxxl = SizedBox(height: xxxxl);

  // ================================
  // 🎯 ГОРИЗОНТАЛЬНЫЕ ОТСТУПЫ - ВИДЖЕТЫ
  // ================================

  /// Горизонтальный очень маленький отступ (2px) - минимальное горизонтальное разделение
  static const SizedBox hxs = SizedBox(width: xs);

  /// Горизонтальный маленький отступ (4px) - плотное горизонтальное расстояние
  static const SizedBox hsm = SizedBox(width: sm);

  /// Горизонтальный средний отступ (8px) - стандартное горизонтальное расстояние
  static const SizedBox hmd = SizedBox(width: md);

  /// Горизонтальный большой отступ (12px) - комфортное горизонтальное разделение
  static const SizedBox hlg = SizedBox(width: lg);

  /// Горизонтальный очень большой отступ (16px) - разделение горизонтальных секций
  static const SizedBox hxl = SizedBox(width: xl);

  /// Горизонтальный 2x большой отступ (24px) - значительное горизонтальное разделение
  static const SizedBox hxxl = SizedBox(width: xxl);

  /// Горизонтальный 3x большой отступ (32px) - максимальное горизонтальное разделение
  static const SizedBox hxxxl = SizedBox(width: xxxl);

  /// Горизонтальный 4x большой отступ (48px) - экстремальное горизонтальное разделение
  static const SizedBox hxxxxl = SizedBox(width: xxxxl);

  // ================================
  // 🛠️ УТИЛИТНЫЕ МЕТОДЫ ДЛЯ АДАПТИВНОСТИ
  // ================================

  /// Получить адаптивный отступ на основе ширины экрана
  ///
  /// ## 🎯 ЛУЧШИЕ ПРАКТИКИ:
  /// - Используйте для responsive дизайна
  /// - Учитывайте плотность пикселей устройства
  /// - Тестируйте на разных размерах экранов
  static double responsiveGap(double screenWidth) {
    if (screenWidth < 360) return sm; // Маленькие экраны (<360px)
    if (screenWidth < 600) return md; // Средние экраны (360px-600px)
    return lg; // Большие экраны (>600px)
  }

  /// Отступ для разделения секций контента
  ///
  /// Используется для разделения major блоков контента на странице
  static double get sectionSpacing => xl;

  /// Отступ для группировки связанных элементов
  ///
  /// Используется для визуальной группировки связанных UI элементов
  static double get groupSpacing => lg;

  /// Отступ между тесно связанными элементами
  ///
  /// Используется для элементов, которые логически связаны, но требуют визуального разделения
  static double get relatedSpacing => md;
}
