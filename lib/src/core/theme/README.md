# theme/

## Назначение
Папка `theme` содержит конфигурацию тем приложения, включая цвета, шрифты, стили текста и общие визуальные константы.

## Структура
**theme/** - 🎨 Тема приложения
- app_theme.dart - 🎯 Основная конфигурация темы
- colors.dart - 🎨 Цветовая палитра
- text_styles.dart - 📝 Текстовые стили
- dark_theme.dart - 🌙 Темная тема
- light_theme.dart - ☀️ Светлая тема
- theme_controller.dart - 🎛️ Контроллер темы
- gap.dart - 📏 Отступы и spacing

## Описание файлов

### app_theme.dart
Основной файл темы приложения.

Содержит:
- базовый класс AppTheme
- конфигурацию MaterialTheme
- настройки для светлой и темной тем
- методы для копирования темы

### colors.dart
Цветовая палитра приложения.

Содержит:
- основные цвета бренда
- семантические цвета (success, warning, error)
- нейтральные цвета (серые оттенки)
- константы для градиентов

### text_styles.dart
Текстовые стили приложения.

Содержит:
- typography систему
- стили для заголовков
- стили для body текста
- стили для кнопок и labels
- утилиты для работы со шрифтами

### dark_theme.dart
Конфигурация темной темы.

Содержит:
- цветовую схему для dark mode
- настройки для темного интерфейса
- специфичные стили для темной темы

### light_theme.dart
Конфигурация светлой темы.

Содержит:
- цветовую схему для light mode
- настройки для светлого интерфейса
- специфичные стили для светлой темы

### theme_controller.dart
Контроллер для управления темой.

Содержит:
- логику переключения между темами
- сохранение выбранной темы
- уведомление об изменении темы

### gap.dart
Утилиты для отступов и spacing.

Содержит:
- константы для отступов
- утилитарные виджеты для spacing
- методы для responsive отступов

## Преимущества

- Единообразие визуального стиля
- Легкое переключение между темами
- Централизованное управление стилями
- Поддержка accessibility
- Быстрая адаптация дизайна

## Best Practices

1. Используйте цвета из палитры, а не хардкод
2. Соблюдайте иерархию текстовых стилей
3. Настройте контрастность для accessibility
4. Используйте семантические цвета для состояний
5. Тестируйте темы на разных устройствах
6. Сохраняйте выбор темы пользователя
7. Используйте константы для отступов
8. Поддерживайте как light, так и dark темы

## Примеры использования

```dart
// Использование цветов
Container(
  color: AppColors.primary,
  child: Text(
    'Hello',
    style: AppTextStyles.headlineLarge,
  ),
)

// Использование темы
final theme = Theme.of(context);
return MaterialApp(
  theme: AppTheme.light,
  darkTheme: AppTheme.dark,
  themeMode: ThemeMode.system,
);

// Кастомные текстовые стили
Text(
  'Заголовок',
  style: AppTextStyles.headlineMedium.copyWith(
    color: AppColors.onSurface,
  ),
)

// Использование отступов
Column(
  children: [
    Text('Элемент 1'),
    Gap.h16,
    Text('Элемент 2'),
    Gap.h24,
  ],
)

// Переключение темы
final themeController = ThemeController();
themeController.toggleTheme();

// Цветовая система
// Основные цвета
primary: Color(0xFF0066FF),
primaryContainer: Color(0xFF004DB8),
onPrimary: Color(0xFFFFFFFF),

// Семантические цвета
success: Color(0xFF00C853),
warning: Color(0xFFFFC107),
error: Color(0xFFD32F2F),

// Нейтральные цвета
surface: Color(0xFFFFFFFF),
onSurface: Color(0xFF1A1A1A),
background: Color(0xFFF5F5F5),

// Текстовая система
// Заголовки
headlineLarge: TextStyle(
  fontSize: 32,
  fontWeight: FontWeight.bold,
  height: 1.2,
),

// Body текст
bodyMedium: TextStyle(
  fontSize: 16,
  fontWeight: FontWeight.normal,
  height: 1.5,
),

// Кнопки
labelLarge: TextStyle(
  fontSize: 14,
  fontWeight: FontWeight.w600,
  letterSpacing: 0.5,
),