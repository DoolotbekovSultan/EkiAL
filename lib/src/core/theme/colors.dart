// ================================
// 🎨 APP COLORS - ЦВЕТОВАЯ СИСТЕМА ДИЗАЙНА
// ================================

import 'package:flutter/material.dart';

/// 🎯 ЦВЕТОВАЯ СИСТЕМА ДИЗАЙНА ПРИЛОЖЕНИЯ
///
/// Реализует полную цветовую палитру на основе дизайн-системы.
///
/// ## 🎯 ЛУЧШИЕ ПРАКТИКИ ИСПОЛЬЗОВАНИЯ:
/// - ✅ ВСЕГДА используйте цвета из этой палитры
/// - ✅ Соблюдайте контрастность для accessibility (WCAG AA)
/// - ✅ Используйте semantic colors для состояний (success/error/warning)
/// - ✅ Тестируйте цвета на разных устройствах и в разных условиях освещения
/// - ❌ НЕ хардкодите цвета в коде
/// - ❌ НЕ используйте произвольные hex-коды
///
/// ## 🎨 СТРУКТУРА ЦВЕТОВОЙ СИСТЕМЫ:
/// - Primary Colors - Основные цвета бренда
/// - Semantic Colors - Семантические цвета для состояний
/// - Neutral Colors - Нейтральная палитра для текста и фонов
/// - Surface Colors - Цвета поверхностей и контейнеров
class AppColors {
  AppColors._();

  // ================================
  // 🎯 PRIMARY COLORS - ОСНОВНЫЕ ЦВЕТА БРЕНДА
  // ================================

  /// 🔵 Основной цвет бренда - используется для primary действий и акцентов
  ///
  /// ## Использование:
  /// - Primary кнопки
  /// - Активные элементы
  /// - Важные акценты
  /// - Brand идентификация
  static const Color primary = Color(0xFF0066FF);

  /// 🔵 Светлый вариант основного цвета - для hover и pressed состояний
  static const Color primaryLight = Color(0xFF66A3FF);

  /// 🔵 Темный вариант основного цвета - для активных состояний
  static const Color primaryDark = Color(0xFF004DB8);

  /// 🔵 Контейнер для основного цвета - для фонов и subtle акцентов
  static const Color primaryContainer = Color(0xFFDAE6FF);

  // ================================
  // 🟣 SECONDARY COLORS - ВТОРОСТЕПЕННЫЕ ЦВЕТА
  // ================================

  /// 🟣 Второстепенный цвет - используется для secondary действий
  ///
  /// ## Использование:
  /// - Secondary кнопки
  /// - Дополнительные акценты
  /// - Вспомогательные элементы
  static const Color secondary = Color(0xFF7C4DFF);

  /// 🟣 Светлый вариант второстепенного цвета
  static const Color secondaryLight = Color(0xFFB47CFF);

  /// 🟣 Темный вариант второстепенного цвета
  static const Color secondaryDark = Color(0xFF3F1DCB);

  /// 🟣 Контейнер для второстепенного цвета
  static const Color secondaryContainer = Color(0xFFE9DDFF);

  // ================================
  // 📊 SEMANTIC COLORS - СЕМАНТИЧЕСКИЕ ЦВЕТА
  // ================================

  /// ✅ Цвет успеха - для позитивных действий и подтверждений
  ///
  /// ## Использование:
  /// - Успешные операции
  /// - Подтверждения
  /// - Positive уведомления
  /// - Статус "выполнено"
  static const Color success = Color(0xFF00C853);

  /// ✅ Светлый вариант цвета успеха
  static const Color successLight = Color(0xFF5EFC82);

  /// ✅ Темный вариант цвета успеха
  static const Color successDark = Color(0xFF009624);

  /// ✅ Контейнер для цвета успеха
  static const Color successContainer = Color(0xFFCCFFD8);

  /// ⚠️ Цвет предупреждения - для предупреждающих сообщений
  ///
  /// ## Использование:
  /// - Предупреждения
  /// - Внимание требуемые действия
  /// - Intermediate статусы
  static const Color warning = Color(0xFFFFC107);

  /// ⚠️ Светлый вариант цвета предупреждения
  static const Color warningLight = Color(0xFFFFF350);

  /// ⚠️ Темный вариант цвета предупреждения
  static const Color warningDark = Color(0xFFC79100);

  /// ⚠️ Контейнер для цвета предупреждения
  static const Color warningContainer = Color(0xFFFFF4C8);

  /// ❌ Цвет ошибки - для ошибок и опасных действий
  ///
  /// ## Использование:
  /// - Ошибки валидации
  /// - Опасные действия
  /// - Negative уведомления
  /// - Статус "ошибка"
  static const Color error = Color(0xFFD32F2F);

  /// ❌ Светлый вариант цвета ошибки
  static const Color errorLight = Color(0xFFEF5350);

  /// ❌ Темный вариант цвета ошибки
  static const Color errorDark = Color(0xFFC62828);

  /// ❌ Контейнер для цвета ошибки
  static const Color errorContainer = Color(0xFFFFDAD6);

  /// ℹ️ Цвет информации - для информационных сообщений
  ///
  /// ## Использование:
  /// - Информационные уведомления
  /// - Подсказки
  /// - Neutral статусы
  static const Color info = Color(0xFF2196F3);

  /// ℹ️ Светлый вариант цвета информации
  static const Color infoLight = Color(0xFF64B5F6);

  /// ℹ️ Темный вариант цвета информации
  static const Color infoDark = Color(0xFF1976D2);

  /// ℹ️ Контейнер для цвета информации
  static const Color infoContainer = Color(0xFFD3EAFD);

  // ================================
  // ⚫️ NEUTRAL COLORS - НЕЙТРАЛЬНАЯ ПАЛИТРА
  // ================================

  /// 🌫️ Нейтральные цвета - для текста, границ и фонов
  ///
  /// ## 🎯 ЛУЧШИЕ ПРАКТИКИ:
  /// - 100-300: Фоны и subtle элементы
  /// - 400-600: Borders и разделители
  /// - 700-900: Текст и важные элементы

  /// Нейтральный 100 - самый светлый фон
  static const Color neutral100 = Color(0xFFF8F9FA);

  /// Нейтральный 200 - светлый фон
  static const Color neutral200 = Color(0xFFE9ECEF);

  /// Нейтральный 300 - средний фон
  static const Color neutral300 = Color(0xFFDEE2E6);

  /// Нейтральный 400 - светлые границы
  static const Color neutral400 = Color(0xFFCED4DA);

  /// Нейтральный 500 - средние границы
  static const Color neutral500 = Color(0xFFADB5BD);

  /// Нейтральный 600 - темные границы и secondary текст
  static const Color neutral600 = Color(0xFF6C757D);

  /// Нейтральный 700 - основной текст
  static const Color neutral700 = Color(0xFF495057);

  /// Нейтральный 800 - заголовки и важный текст
  static const Color neutral800 = Color(0xFF343A40);

  /// Нейтральный 900 - самый темный текст
  static const Color neutral900 = Color(0xFF212529);

  // ================================
  // 🖼️ SURFACE & BACKGROUND COLORS
  // ================================

  /// 🖼️ Основной фон приложения
  ///
  /// Используется для основного фона Scaffold и страниц
  static const Color background = Color(0xFFFAFBFD);

  /// 🖼️ Цвет поверхностей - для карточек, панелей и контейнеров
  static const Color surface = Color(0xFFFFFFFF);

  /// 🖼️ Вариант поверхности - для elevated контейнеров
  static const Color surfaceVariant = Color(0xFFF1F3F9);

  // ================================
  // ✍️ ON COLORS - ЦВЕТА ДЛЯ ТЕКСТА НА ФОНЕ
  // ================================

  /// ✍️ Цвет текста на основном цвете
  static const Color onPrimary = Color(0xFFFFFFFF);

  /// ✍️ Цвет текста на второстепенном цвете
  static const Color onSecondary = Color(0xFFFFFFFF);

  /// ✍️ Цвет текста на фоне
  static const Color onBackground = Color(0xFF1A1A1A);

  /// ✍️ Цвет текста на поверхности
  static const Color onSurface = Color(0xFF1A1A1A);

  /// ✍️ Цвет текста на цвете ошибки
  static const Color onError = Color(0xFFFFFFFF);

  // ================================
  // 🛠️ УТИЛИТНЫЕ МЕТОДЫ
  // ================================

  /// Конвертировать hex строку в Color
  ///
  /// ## Пример использования:
  /// ```dart
  /// final color = AppColors.fromHex('#FF0066');
  /// ```
  static Color fromHex(String hexString) {
    final buffer = StringBuffer();
    if (hexString.length == 6 || hexString.length == 7) buffer.write('ff');
    buffer.write(hexString.replaceFirst('#', ''));
    return Color(int.parse(buffer.toString(), radix: 16));
  }

  /// Конвертировать Color в hex строку
  ///
  /// ## Пример использования:
  /// ```dart
  /// final hex = AppColors.toHex(AppColors.primary);
  /// ```
  static String toHex(Color color, {bool leadingHashSign = true}) {
    return '${leadingHashSign ? '#' : ''}'
        '${color.alpha.toRadixString(16).padLeft(2, '0')}'
        '${color.red.toRadixString(16).padLeft(2, '0')}'
        '${color.green.toRadixString(16).padLeft(2, '0')}'
        '${color.blue.toRadixString(16).padLeft(2, '0')}';
  }
}
