# extensions/

## Назначение
Папка `extensions` содержит расширения для стандартных классов Dart и Flutter, которые добавляют дополнительную функциональность.

## Структура
**extensions/** - 🔌 Расширения классов
- string_extensions.dart - 📝 Расширения String
- datetime_extensions.dart - 📅 Расширения DateTime
- list_extensions.dart - 📋 Расширения List
- context_extensions.dart - 🎯 Расширения BuildContext
- num_extensions.dart - 🔢 Расширения числовых типов
- widget_extensions.dart - 🧩 Расширения Widget
- map_extensions.dart - 🗺️ Расширения Map

## Описание файлов

### string_extensions.dart
Расширения для класса String.

Содержит методы для:
- валидации строк (email, phone, URL)
- форматирования текста
- преобразования регистра
- работы с путями и файлами

### datetime_extensions.dart
Расширения для класса DateTime.

Содержит методы для:
- форматирования дат
- вычисления разницы между датами
- проверки периодов
- работы с временными зонами

### list_extensions.dart
Расширения для класса List.

Содержит методы для:
- фильтрации и преобразования
- работы с дубликатами
- пагинации и разделения
- безопасного доступа к элементам

### context_extensions.dart
Расширения для BuildContext.

Содержит методы для:
- доступа к теме и стилям
- навигации и диалогов
- получения размеров экрана
- локализации

### num_extensions.dart
Расширения для числовых типов (int, double).

Содержит методы для:
- форматирования чисел
- преобразования единиц измерения
- математических операций
- работы с процентами

### widget_extensions.dart
Расширения для Widget.

Содержит методы для:
- добавления отступов
- применения стилей
- добавления жестов
- модификации свойств

### map_extensions.dart
Расширения для Map.

Содержит методы для:
- глубокого копирования
- фильтрации и преобразования
- безопасного доступа к значениям
- сериализации

## Преимущества

- Уменьшение дублирования кода
- Улучшение читаемости
- Единообразие операций
- Безопасные операции с null-безопасностью
- Переиспользуемость функциональности

## Best Practices

1. Создавайте расширения для часто повторяющихся операций
2. Называйте методы понятно и интуитивно
3. Добавляйте документацию к сложным расширениям
4. Тестируйте расширения как отдельные единицы
5. Избегайте side effects в расширениях
6. Используйте null-безопасные методы (firstOrNull, getOrNull)
7. Группируйте связанные расширения в одном файле
8. Не злоупотребляйте расширениями для простых операций

## Примеры использования

```dart
// String расширения
if (email.isValidEmail) {
  // валидный email
}

final capitalized = 'hello world'.capitalize; // 'Hello world'

// DateTime расширения
final formatted = DateTime.now().toFormattedString(); // '15 дек 2023'
final isToday = someDate.isToday;

// List расширения
final uniqueList = listWithDuplicates.unique();
final firstOrNull = emptyList.firstOrNull;

// Context расширения
final theme = context.theme;
final isDark = context.isDarkMode;
context.showSnackBar('Message');

// Num расширения
final formattedPrice = 1000.0.toPriceString(); // '1 000 ₽'
final percent = 0.15.toPercentString(); // '15%'

// Widget расширения
Container()
  .withPadding(16)
  .withBackground(Colors.blue)
  .withBorderRadius(8);

// Map расширения
final value = someMap.getStringOrNull('key');
final deepCopy = originalMap.deepCopy();