# config/

## Назначение
Папка `config` содержит конфигурацию приложения для разных окружений (development, staging, production). Конфигурация находится в core слое, так как используется другими компонентами core (network, di).

## Структура
**config/** - ⚙️ Конфигурация приложения
- app_config.dart - 🎯 Базовый абстрактный класс конфигурации
- config_reader.dart - 📖 Утилиты для работы с конфигурацией
- dev_config.dart - 🛠️ Development окружение
- staging_config.dart - 🧪 Staging окружение
- prod_config.dart - 🚀 Production окружение

## Описание файлов

### app_config.dart
Абстрактный класс конфигурации приложения.

**Содержит:**
- Базовые настройки (baseUrl, apiKey, isDebug, environmentName)
- Настройки разработки (enableDebugTools, useMockData, logLevel)
- Настройки мониторинга (enableAnalytics, enableCrashReporting, enablePerformanceMonitoring)
- Настройки кэширования (enableCaching, cacheDuration, maxCacheSize)
- Фабричный метод для создания конфигурации на основе окружения
- Валидацию конфигурации

**Пример использования:**
```dart
final config = AppConfig();
final baseUrl = config.baseUrl;
final isDebug = config.isDebug;
```

### config_reader.dart
Утилиты для работы с конфигурацией.

**Содержит:**
- Валидацию конфигурации
- Логирование настроек
- Утилиты для работы с окружениями
- Безопасное логирование (маскирование sensitive данных)

**Пример использования:**
```dart
ConfigReader.validateConfig(config);
ConfigReader.logSummaryConfig(config);
final isProd = ConfigReader.isProduction(config);
```

### dev_config.dart
Конфигурация для development окружения.

**Особенности:**
- Локальные серверы и мок-данные
- Расширенное логирование
- Отключена аналитика
- Включены debug tools

### staging_config.dart
Конфигурация для staging окружения.

**Особенности:**
- Тестовые серверы
- Баланс между логированием и производительностью
- Тестовая аналитика
- Реальные данные (но тестовые)

### prod_config.dart
Конфигурация для production окружения.

**Особенности:**
- Production серверы
- Минимальное логирование
- Включена аналитика и мониторинг
- Оптимизированные таймауты

## Использование

### Получение конфигурации
```dart
// Автоматический выбор на основе APP_ENVIRONMENT
final config = AppConfig();

// Или через singleton
final config = AppConfig.current;
```

### Переключение окружений
```bash
# Development (по умолчанию)
flutter run

# Staging
flutter run --dart-define=APP_ENVIRONMENT=staging

# Production
flutter run --dart-define=APP_ENVIRONMENT=production
```

### Валидация конфигурации
```dart
try {
  config.validate();
  config.logConfig();
} on ConfigValidationException catch (e) {
  // Обработка ошибок валидации
}
```

## Архитектурные принципы

- ✅ **Core слой** - конфигурация находится в core, так как используется другими компонентами core
- ✅ **Независимость** - core не зависит от app слоя
- ✅ **Type-safe** - использование абстрактных классов и интерфейсов
- ✅ **Валидация** - проверка корректности настроек при инициализации
- ✅ **Безопасность** - маскирование sensitive данных в логах

## Best Practices

1. Используйте `AppConfig.current` для получения текущей конфигурации
2. Всегда валидируйте конфигурацию при запуске приложения
3. Не логируйте sensitive данные (API ключи, токены)
4. Используйте environment variables для production ключей
5. Тестируйте конфигурации для всех окружений

