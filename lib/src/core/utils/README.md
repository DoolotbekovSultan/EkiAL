# utils/

## Назначение
Папка `utils` содержит вспомогательные функции, утилиты и хелперы для различных задач приложения.

## Используемые библиотеки
- **intl** - форматирование дат, чисел и валют
- **logger** - логирование и мониторинг
- **flutter** - работа с устройством и платформой
- **cached_network_image** - работа с изображениями (опционально)
- **decimal** - точные денежные вычисления (опционально)

## Структура
**utils/** - 🛠️ Вспомогательные утилиты
- validators.dart - ✅ Валидация данных
- formatters.dart - 📝 Форматирование данных
- date_utils.dart - 📅 Работа с датами
- device_utils.dart - 📱 Работа с устройством
- log_utils.dart - 📋 Логирование
- debouncer.dart - ⏰ Debounce операции
- money_utils.dart - 💰 Работа с деньгами
- image_utils.dart - 🖼️ Работа с изображениями

## Описание файлов

### validators.dart
Утилиты для валидации данных.

**Использует:** регулярные выражения (RegExp), базовые типы Dart

Содержит:
- валидацию email (RegExp)
- валидацию телефона (RegExp)
- валидацию пароля (политики сложности)
- проверку обязательных полей
- кастомные валидаторы форм

### formatters.dart
Утилиты для форматирования данных.

**Использует:** `intl`, `flutter/services`

Содержит:
- форматирование чисел (NumberFormat)
- форматирование дат (DateFormat)
- маски для ввода данных (TextInputFormatter)
- преобразование регистров
- обрезку и обработку строк

### date_utils.dart
Утилиты для работы с датами и временем.

**Использует:** `intl`, базовые типы Dart

Содержит:
- парсинг дат из строк (DateFormat)
- форматирование дат для отображения (DateFormat)
- вычисление разницы между датами (DateTime)
- работу с временными зонами
- утилиты для календаря

### device_utils.dart
Утилиты для работы с устройством.

**Использует:** `flutter/foundation`, `flutter/services`, `dart:io`

Содержит:
- определение типа устройства (MediaQuery, Platform)
- проверку версии ОС (Platform, DeviceInfo)
- работу с платформой (Platform, TargetPlatform)
- утилиты для клавиатуры (MediaQuery, FocusScope)
- информацию о размерах экрана (MediaQuery)

### log_utils.dart
Утилиты для логирования.

**Использует:** `logger`

Содержит:
- настройку логгера (Logger, PrettyPrinter)
- фильтрацию логов по уровням (Level)
- форматирование логов (LogPrinter)
- логирование в файл (FileOutput)

### debouncer.dart
Утилита для debounce операций.

**Использует:** `dart:async`

Содержит:
- отложенное выполнение функций (Timer)
- отмену предыдущих операций (Timer.cancel())
- настройку времени задержки (Duration)

### money_utils.dart
Утилиты для работы с деньгами.

**Использует:** `intl`, `decimal` (опционально)

Содержит:
- форматирование валют (NumberFormat)
- округление денежных значений
- конвертацию валют
- работу с копейками/центами

### image_utils.dart
Утилиты для работы с изображениями.

**Использует:** `cached_network_image`, `flutter/painting`, `dart:io`

Содержит:
- загрузку и кэширование изображений (CachedNetworkImage)
- обработку изображений (ImageProvider)
- работу с аватарами (CircleAvatar, ClipRRect)
- утилиты для иконок (Icon)

## Преимущества

- Уменьшение дублирования кода
- Единообразие операций
- Упрощение тестирования
- Централизованное управление утилитами
- Легкое поддержание и обновление

## Best Practices

1. Создавайте утилиты для часто повторяющихся операций
2. Делайте утилиты чистыми функциями без side effects
3. Добавляйте документацию к сложным утилитам
4. Тестируйте утилиты как отдельные модули
5. Используйте параметры для настройки поведения
6. Обрабатывайте edge cases и ошибки
7. Следите за производительностью утилит
8. Разделяйте утилиты по функциональности

## Примеры использования

```dart
// Валидация
final isValidEmail = Validators.isEmail('test@example.com');
final isValidPhone = Validators.isPhone('+79991234567');

// Форматирование
final formattedDate = DateUtils.formatDateTime(DateTime.now());
final formattedPrice = MoneyUtils.formatPrice(1500.50); // 1 500,50 ₽

// Логирование
Logger.info('User logged in', {'userId': '123'});
Logger.error('API request failed', error, stackTrace);

// Debounce для поиска
final debouncer = Debouncer(milliseconds: 500);
debouncer.run(() {
  searchProducts(query);
});

// Форматирование ввода
final phoneFormatter = PhoneInputFormatter();
final maskedPhone = phoneFormatter.formatEditUpdate(
  TextEditingValue.empty,
  TextEditingValue(text: '79991234567'),
);

// Работа с устройством
final isTablet = DeviceUtils.isTablet(context);
final platform = DeviceUtils.platform;
final screenSize = DeviceUtils.screenSize(context);

// Форматирование данных
final capitalized = Formatters.capitalize('hello world'); // 'Hello World'
final trimmed = Formatters.trimAndRemoveExtraSpaces('  hello   world  '); // 'hello world'