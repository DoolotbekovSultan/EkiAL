# datasources/

## Назначение
Папка `datasources` содержит общие источники данных, которые используются несколькими фичами приложения. Отвечает за получение и сохранение общих данных из различных внешних источников.

## Используемые библиотеки
- **dio** - для сетевых запросов к общим API endpoints
- **shared_preferences** - для общих настроек приложения
- **hive/isar** - для общего локального хранилища данных

## Структура
**datasources/** - 🌐 Источники данных
- remote_data_source.dart - 🌐 Удаленные данные (API)
- local_data_source.dart - 💾 Локальные данные (хранилище)
- cache_data_source.dart - ⚡ Кэшированные данные

## Описание файлов

### remote_data_source.dart
Удаленный источник данных пользователя. Содержит:
- Методы для получения данных пользователя
- Обновление профиля пользователя
- Работу с аватарами и медиа
- Аутентификационные запросы

**Используемые библиотеки в файле:**
- `dio` - Dio, BaseOptions для HTTP запросов

### local_data_source.dart
Локальный источник настроек приложения. Содержит:
- Сохранение и загрузку настроек
- Управление темами и локализацией
- Кэширование пользовательских предпочтений
- Миграции настроек между версиями

**Используемые библиотеки в файле:**
- `shared_preferences` - SharedPreferences для key-value хранилища
- `hive` - Box (если используется Hive)

### cache_data_source.dart
Источник кэшированных данных. Содержит:
- Временное хранение общих данных
- Стратегии инвалидации кэша
- Приоритеты кэширования для разных типов данных
- Очистку устаревших данных

**Используемые библиотеки в файле:**
- `hive/isar` - для persistent кэша

## Преимущества

- **Единый источник** - согласованные данные во всех фичах
- **Оптимизация** - избежание дублирования запросов
- **Согласованность** - одинаковая логика работы с данными
- **Производительность** - общее кэширование между фичами
- **Поддерживаемость** - централизованное управление источниками

## Best Practices

1. Используйте для данных, которые действительно нужны в нескольких фичах
2. Следите за стабильностью API общих datasources
3. Документируйте предназначение каждого общего источника
4. Минимизируйте зависимости от конкретных фич
5. Используйте appropriate error handling для общих операций
6. Тестируйте общие datasources изолированно
7. Следите за performance impact общих операций
8. Используйте стратегии кэширования подходящие для общих данных

## Примеры использования

```dart
// user_remote_data_source.dart
class UserRemoteDataSource {
  final Dio _dio;

  UserRemoteDataSource(this._dio);

  Future<UserModel> getCurrentUser() async {
    final response = await _dio.get('/user/current');
    return UserModel.fromJson(response.data);
  }
}

// settings_local_data_source.dart
class SettingsLocalDataSource {
  final SharedPreferences _prefs;

  SettingsLocalDataSource(this._prefs);

  Future<void> saveThemeMode(ThemeMode mode) async {
    await _prefs.setString('theme_mode', mode.toString());
  }

  ThemeMode getThemeMode() {
    final mode = _prefs.getString('theme_mode');
    return ThemeMode.values.firstWhere(
      (e) => e.toString() == mode,
      orElse: () => ThemeMode.system,
    );
  }
}