# Mappers

## Назначение
Папка `mappers` содержит общие преобразователи данных, которые используются несколькими фичами приложения. Отвечает за конвертацию между различными представлениями данных.

## Структура
**mappers/** - 🔄 Преобразователи данных
- shared_mapper.dart - 🎯 Базовые мапперы и интерфейсы

## Описание

### shared_mapper.dart
Абстрактный преобразователь данных. Содержит:
- Базовый интерфейс для всех мапперов
- Общие методы преобразования
- Утилиты для работы с коллекциями
- Обработку ошибок маппинга

## Основные компоненты

### Mapper<TFrom, TTo>
Абстрактный класс маппера, определяющий:
- `TTo map(TFrom from)` - преобразование одного объекта
- `List<TTo> mapList(List<TFrom> from)` - преобразование списка
- `TFrom reverseMap(TTo to)` - обратное преобразование
- Валидацию входных данных

## Преимущества

- **Стандартизация** - единый подход к преобразованию данных
- **Переиспользуемость** - общая логика для всех мапперов
- **Тестируемость** - четкие контракты для тестирования
- **Расширяемость** - легко добавлять новые мапперы
- **Поддерживаемость** - централизованная логика преобразований

## Best Practices

1. Наследуйте все мапперы от базового Mapper
2. Реализуйте как прямое, так и обратное преобразование
3. Валидируйте входные данные перед преобразованием
4. Используйте отдельные мапперы для сложных преобразований
5. Документируйте правила преобразования для каждого маппера
6. Тестируйте граничные случаи и ошибки
7. Следите за неизменяемостью данных где это возможно

## Пример использования

```dart
// Базовый маппер
abstract class Mapper<TFrom, TTo> {
  TTo map(TFrom from);
  
  List<TTo> mapList(List<TFrom> from) {
    return from.map((item) => map(item)).toList();
  }
  
  TFrom reverseMap(TTo to);
}

// Конкретная реализация
class UserMapper extends Mapper<UserDTO, UserEntity> {
  @override
  UserEntity map(UserDTO from) {
    return UserEntity(
      id: from.id,
      name: from.name,
      email: from.email,
    );
  }
  
  @override
  UserDTO reverseMap(UserEntity to) {
    return UserDTO(
      id: to.id,
      name: to.name,
      email: to.email,
    );
  }
}