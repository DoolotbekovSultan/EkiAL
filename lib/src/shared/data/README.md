# data/

## Назначение
Папка `data` в shared слое содержит общие реализации работы с данными, которые используются несколькими фичами приложения.

## Используемые библиотеки
- **dio** - для сетевых запросов (если используются общие API)
- **hive/isar** - для общего локального хранилища
- **shared_preferences** - для общих настроек приложения

## Структура
**data/** - 💾 Слой данных
- datasources/ - 🌐 Источники данных (local/remote/cache)
- models/ - 📊 DTO модели данных
- repositories/ - 🔄 Репозитории данных
- mapper/ - 🔄 Преобразователи данных

## Описание директорий

### datasources/
Общие источники данных. Содержит:
- API клиенты для общих endpoints
- Локальное хранилище общих данных
- Кэш для данных, используемых в разных фичах

**Используемые библиотеки в файлах:**
- `dio` - для сетевых запросов
- `hive/isar` - для локальной БД

### models/
Общие модели данных. Содержит:
- DTO для данных, используемых в нескольких фичах
- Модели пользователя, настроек, конфигураций
- Базовые модели ответов API

**Используемые библиотеки в файлах:**
- `json_annotation` - для сериализации
- `freezed` - для immutable классов

### repositories/
Реализации общих репозиториев. Содержит:
- Репозитории для пользовательских данных
- Репозитории для настроек приложения
- Репозитории для кэширования общих данных

**Используемые библиотеки в файлах:**
- `dartz` - для обработки результатов

### mapper/
Общие преобразователи данных. Содержит:
- Мапперы для общих сущностей
- Конвертацию между общими моделями и сущностями
- Нормализацию общих данных

## Преимущества

- **Единый источник истины** - согласованные данные across фич
- **Оптимизация** - избежание дублирования сетевых запросов
- **Согласованность** - единая логика работы с данными
- **Производительность** - общее кэширование данных
- **Поддерживаемость** - централизованное управление общими данными

## Best Practices

1. Используйте для данных, которые действительно нужны в нескольких фичах
2. Следите за стабильностью интерфейсов общих репозиториев
3. Документируйте предназначение каждого общего data компонента
4. Минимизируйте зависимости от конкретных фич
5. Используйте clear contracts для общих data источников
6. Тестируйте общие data компоненты изолированно
7. Следите за performance impact общих data операций
8. Используйте appropriate caching strategies для общих данных