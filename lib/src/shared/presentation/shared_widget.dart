import 'package:flutter/material.dart';

class SharedWidget extends StatelessWidget {
  const SharedWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text('Shared Widget'));
  }
}
